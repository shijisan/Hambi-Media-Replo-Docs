import type { NextConfig } from 'next'

const nextConfig: NextConfig = {
  images: {
    domains: ["lh3.googleusercontent.com", "placehold.co"], // or whatever your user.image domain is
  },
}

export default nextConfig